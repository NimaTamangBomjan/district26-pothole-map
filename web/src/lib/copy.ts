/**
 * UI copy strings. Placeholders for now — update the text here.
 *
 * Three distinct context paragraphs:
 *  - DISTRICT_CONTEXT: under the stats in the District (D26) view.
 *  - ZIP_CONTEXT: under the ranks in the Zip Code (detail) view. Different copy
 *    from the district view.
 *  - LAYERS_CONTEXT: beside the layer selector — the desktop bottom band and the
 *    mobile Layers modal.
 */
export const DISTRICT_CONTEXT =
  '<b>Counts</b> of green infrastructure assets, 311 service requests, and catch basins are per square mile. <br><b>Mean</b> flood vulnerability averages the estimated vulnerability across District 26 between 1 (less vulnerable) and 5 (more vulnerable). <br><b>Percentages</b> represent the area of District 26 covered by each layer.';

export const ZIP_CONTEXT =
  'The rankings above compare the selected zip code to the others that intersect with District 26. <br>The <b>smallest</b> counts or amounts of a characteristic across the zip codes are represented by a rank of <b>one out of five.</b><br>The <b>largest</b> counts or amounts are represented by a rank of <b>five out five.</b>';

export const LAYERS_CONTEXT =
  'View selected green infrastructure assets and flood-related layers in and around District 26.';
